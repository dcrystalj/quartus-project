module full_adder (x1, x2, c_in, c_out, r);

	input x1, x2, c_in;
	output c_out, r;
	
	wire w1, c1, c2;
	half_adder ha1( .x1(x1), .x2(x2), .c(c1), .r(w1));
	half_adder ha2( .x1(w1), .x2())
endmodule